#include<stdio.h>
int main()
{
	int i,n,sd=0,r;
	printf("enter n");
	scanf("%d",&n);
	while(n>0)
     {
     	r=n%10;
     	sd=sd+r*r*r;
     	n=n/10;
	 }
 printf("sum of digits=%d",sd);
 return 0;

}
